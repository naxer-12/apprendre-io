{
  "name": "apprendre-io",
  "version": "1.0.0",
  "private": true,
  "description": "Evidence-backed French-learning web app — zero-dependency static site, no build step.",
  "scripts": {
    "test": "node test/progress.test.js && node test/auth.test.js && node test/data.test.js && node test/e2e-simulation.test.js",
    "start": "python3 -m http.server 8085"
  }
}
